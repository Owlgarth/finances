# Project
.env
.venv
backend/.venv
backend/.env
.env
*.env
!example.env
IMPLEMENTATION_PLAN.md
opencode.json

# OS
.DS_Store

# IDE
.idea/

# AI Tools
.plans
.opencode-data/
# .agents/: only the shared skills and settings are tracked. Note the `/*`:
# excluding the directory itself would stop git descending into it, and the
# negations would never be reached.
.agents/*
!.agents/skills/
!.agents/settings.json

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python

# Compiled translation catalogs (built by compilemessages in the entrypoints)
backend/locale/**/*.mo

# Node
frontend/node_modules/
frontend/yarn.lock
node_modules/
dist/
build/
*.log
